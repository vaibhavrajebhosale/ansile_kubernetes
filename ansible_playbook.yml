- hosts: all
  remote_user: edureka
  gather_facts: no

  tasks:
    - name: building docker image using war file
      command: docker image build -t vaibhav/ABCtechnologies-image .
      args:
        chdir: /home/edureka/ansible_project1

